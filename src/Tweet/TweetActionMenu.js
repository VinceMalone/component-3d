import styled from "styled-components";

const Container = styled.div`
  color: rgba(0 0 0 / 0.87);
  font-family: serif;
  line-height: 1;
`;

export function TweetActionMenu() {
  return <Container>⋯</Container>;
}
